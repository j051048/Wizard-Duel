# Wizard Duel 代码改良与重构路线图
> 基于《全面审计报告》制定的分阶段实施计划

## 📅 阶段一：核心规则稳固 (Critical Fixes)
**目标**：消除P0级别严重的逻辑漏洞，确保规则执行的一致性与稳定性，保证游戏“不出错”。

#### 1. 规则引擎修复
- [ ] **修复死亡结算时机** [P0]
    - 在 `prepareNextTurn` 的每个关键步骤（DoT结算、抽牌后）插入死亡检查。
    - 确保 DoT 伤害导致死亡后，不应继续执行回血或抽牌逻辑。
- [ ] **修复状态绕过漏洞** [P0]
    - 在 `executeSpell` 中将 `frozen` (冻结) 检查移至最顶层，确保英雄技能无法绕过冻结。
- [ ] **AI 行为节奏优化** [P0]
    - 在 `executeAITurn` 逻辑中引入 `await delay(1500)`，让AI每张出牌都有“思考时间”，避免瞬间打空手牌。
- [ ] **断线/刷新重连验证** [P0]
    - 确保 `localStorage` 中的 `wizard_duel_save` 能完整恢复战斗状态（包括当前回合数、状态效果剩余回合）。

#### 2. 移动端交互基建
- [ ] **长按与拖拽冲突终极修复** [P0]
    - 审查 `useDragToPlay` 与 `BattleHand` 的事件锁，确保手势不打架。
- [ ] **手牌操作安全校验** [已完成]
    - (已修复) 移动端双重扣费问题。

#### 3. 代码卫生
- [ ] **类型定义拆分**
    - 将 300+行的 `types.ts` 拆分为 `types/card.ts`, `types/state.ts`, `types/ui.ts`。

---

## 🏗️ 阶段二：架构重构与性能 (Architecture Refactor)
**目标**：降低代码复杂度，提升渲染性能，解决 `BattleArena` 难以维护的问题。

#### 1. BattleArena 组件拆解 [P1]
将 600+ 行的巨型组件拆分为独立子文件：
- [ ] `components/battle/hud/PlayerHUD.tsx` (头像、血量、技能)
- [ ] `components/battle/hud/OpponentHUD.tsx` (对手信息)
- [ ] `components/battle/board/BattleField.tsx` (中央战场、及待加入的随从区)
- [ ] `components/battle/hand/HandArea.tsx` (手牌容器与逻辑)

#### 2. 状态管理优化
- [ ] **Context 拆分**
    - 避免从 `gameLoopState` 巨型对象中解构所有属性。
    - 使用 `React.memo` 保护子组件，仅在相关 Props 变化时重绘。
- [ ] **Canvas 性能优化**
    - 移除 `canvas` 对 `window.resize` 的直接高频监听，改用 `ResizeObserver`。

#### 3. 移动端 UI/UX 深度适配
- [ ] **释放区引导优化**
    - 拖拽开始时，高亮区域应立即显现且清晰。
    - 添加持久的箭头指引（不仅仅是拖到一半才显示）。
- [ ] **UI 布局微调**
    - 增大移动端回合结束按钮 (80x80px) 并添加材质。
    - 修复横屏模式下的布局适配。

---

## ✨ 阶段三：体验与视觉打磨 (Polish & Juice)
**目标**：提升表现力，让游戏看起来“商业级”，前90秒留住用户。

#### 1. 视觉反馈 (Juice)
- [ ] **卡牌特效**
    - 可打出手牌增加持续的绿色脉冲光效。
    - 稀有/强力卡牌增加特殊的边框特效。
- [ ] **伤害飘字升级**
    - 实现带物理弹跳、缩放、属性颜色（火=红，冰=蓝）的伤害数字组件。
- [ ] **场景氛围**
    - 背景增加缓慢流动的魔法粒子或迷雾效果。

#### 2. 大厅与编辑器
- [ ] **牌组编辑器优化**
    - 修复移动端底部抽屉遮挡卡牌列表的问题。
    - 增加“一键清空”按钮。
- [ ] **起手换牌 (Mulligan) 优化**
    - 修复倒计时震动过于频繁的问题。
    - 增加“对手正在选择...”的虚拟状态提示。

---

## 💰 阶段四：商业化与系统深度 (Monetization & Systems)
**目标**：完善外围系统，增加付费点和长线游玩动力。

#### 1. 商店与商业化
- [ ] **开包体验重做** [High Value]
    - 实现翻牌动画（点击卡背翻转）。
    - 开出稀有卡时的震动与全屏光效。
- [ ] **首充引导**
    - 在大厅增加“首充双倍”或“新手礼包”入口。

#### 2. 游戏平衡性调整
- [ ] **数值平衡 (Balance Patch v1.0)**
    - 削弱 Thunder 连击倍率。
    - 削弱 Hero Vine (自然呼唤) 的收益。
    - 增强 4-6 费曲线卡牌的价值。

#### 3. 辅助系统
- [ ] **战斗回放/日志**
    - 完善 CombatLog，使其更易读。
- [ ] **任务系统持久化**
    - 确保任务进度在不同会话间正确保存。
