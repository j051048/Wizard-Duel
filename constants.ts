/**
 * @deprecated 核心数据已拆分到 config/gameConfig.ts, data/spells.ts, utils/deckUtils.ts
 * 为了保持向后兼容，此处仅做重新导出。
 * 请在新代码中直接引用新的路径。
 */

export * from './config/gameConfig';
export * from './data/spells';
export * from './utils/deckUtils';
